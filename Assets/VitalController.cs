using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VitalController : MonoBehaviour
{
    // Start is called before the first frame update
    public Animator Vital;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
